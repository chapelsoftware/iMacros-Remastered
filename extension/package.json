{
  "name": "@imacros/extension",
  "version": "1.0.0",
  "description": "iMacros Browser Extension",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "vite build",
    "build:tsc": "tsc --build",
    "build:clean": "tsc --build --clean && rm -rf dist",
    "dev": "vite build --watch"
  },
  "dependencies": {
    "@imacros/shared": "*"
  }
}
